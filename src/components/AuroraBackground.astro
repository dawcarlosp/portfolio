<div class="fixed inset-0 pointer-events-none" style="z-index: -1; contain: paint;">
  <div class="absolute inset-0 bg-gradient-to-b from-[#0B132B] via-[#111133] to-[#0a1a2f]" />

  <div class="absolute inset-0 overflow-hidden aurora-stars-1" />
  <div class="absolute inset-0 overflow-hidden aurora-stars-2" />

  <div class="absolute inset-0 aurora-wave-1" />
  <div class="absolute inset-0 aurora-wave-2" />

  <div class="absolute inset-0 aurora-halo" />

  <svg class="absolute inset-0 w-full h-full" viewBox="0 0 1200 800" preserveAspectRatio="none" fill="none" style="opacity: 0.4;">
    <defs>
      <radialGradient id="deepViolet" cx="50%" cy="40%" r="80%">
        <stop offset="0%" stop-color="#9B51E0" stop-opacity="0.7" />
        <stop offset="100%" stop-color="#6A3093" stop-opacity="0" />
      </radialGradient>
      <radialGradient id="deepBlue" cx="50%" cy="50%" r="80%">
        <stop offset="0%" stop-color="#3B82F6" stop-opacity="0.6" />
        <stop offset="100%" stop-color="#1e3a8a" stop-opacity="0" />
      </radialGradient>
    </defs>
    <path fill="url(#deepViolet)" d="M0,420 C300,340 900,480 1200,400 L1200,800 L0,800 Z" />
    <path fill="url(#deepBlue)" d="M0,460 C400,380 800,510 1200,430 L1200,800 L0,800 Z" />
    <path fill="url(#deepViolet)" d="M0,500 C500,410 700,540 1200,460 L1200,800 L0,800 Z" />
    <path fill="url(#deepBlue)" d="M0,540 C500,450 700,570 1200,490 L1200,800 L0,800 Z" />
  </svg>
</div>

<style>
  .aurora-stars-1::before {
    content: '';
    position: absolute;
    width: 2px;
    height: 2px;
    border-radius: 50%;
    background: transparent;
    box-shadow:
      100px 80px 1.5px #fffacd,
      250px 150px 1px #e0e0ff,
      400px 60px 1.8px #ffffff,
      550px 200px 1.2px #fffacd,
      700px 90px 1.5px #e0e0ff,
      850px 180px 1px #ffffff,
      1000px 70px 1.3px #fffacd,
      1100px 220px 1.6px #e0e0ff,
      150px 350px 1.1px #ffffff,
      500px 400px 0.9px #fffacd,
      900px 350px 1.4px #e0e0ff,
      350px 500px 1px #ffffff,
      750px 520px 1.2px #fffacd,
      1050px 480px 0.8px #e0e0ff,
      50px 600px 1.3px #ffffff,
      620px 650px 1.1px #fffacd;
    animation: twinkle-1 4s ease-in-out infinite alternate;
    will-change: opacity;
  }

  .aurora-stars-2::before {
    content: '';
    position: absolute;
    width: 1.5px;
    height: 1.5px;
    border-radius: 50%;
    background: transparent;
    box-shadow:
      120px 180px 1px #ffffff,
      320px 90px 1.2px #fffacd,
      480px 250px 0.8px #e0e0ff,
      600px 120px 1.4px #ffffff,
      780px 280px 1px #fffacd,
      920px 140px 1.1px #e0e0ff,
      200px 480px 1.3px #ffffff,
      420px 580px 0.9px #fffacd,
      680px 380px 1px #e0e0ff,
      820px 550px 1.2px #ffffff,
      150px 700px 1.5px #fffacd,
      1150px 150px 1px #e0e0ff,
      1020px 350px 1.3px #ffffff,
      280px 620px 0.8px #fffacd;
    animation: twinkle-2 5s ease-in-out infinite alternate;
    will-change: opacity;
  }

  @keyframes twinkle-1 {
    0% { opacity: 0.3; }
    100% { opacity: 1; }
  }

  @keyframes twinkle-2 {
    0% { opacity: 0.4; }
    100% { opacity: 0.9; }
  }

  .aurora-wave-1 {
    background: radial-gradient(ellipse 80% 40% at 50% 0%, rgba(0, 255, 204, 0.12) 0%, transparent 100%);
    animation: wave-drift-1 12s ease-in-out infinite alternate;
    will-change: transform, opacity;
  }

  .aurora-wave-2 {
    background: radial-gradient(ellipse 60% 30% at 50% 0%, rgba(0, 255, 238, 0.08) 0%, transparent 100%);
    animation: wave-drift-2 16s ease-in-out infinite alternate;
    will-change: transform, opacity;
  }

  .aurora-halo {
    background: radial-gradient(ellipse 100% 50% at 50% 0%, rgba(0, 255, 204, 0.06) 0%, transparent 100%);
    animation: wave-drift-1 14s ease-in-out infinite alternate;
    will-change: transform, opacity;
  }

  @keyframes wave-drift-1 {
    0% { transform: translateX(-5%) scaleY(0.95); opacity: 0.6; }
    100% { transform: translateX(5%) scaleY(1.05); opacity: 1; }
  }

  @keyframes wave-drift-2 {
    0% { transform: translateX(3%) scaleY(1.05); opacity: 0.5; }
    100% { transform: translateX(-3%) scaleY(0.95); opacity: 0.9; }
  }

  @media (prefers-reduced-motion: reduce) {
    .aurora-wave-1,
    .aurora-wave-2,
    .aurora-halo {
      animation: none;
      opacity: 0.5;
    }
    .aurora-stars-1::before,
    .aurora-stars-2::before {
      animation: none;
      opacity: 0.6;
    }
  }

  @media (max-width: 768px) {
    .aurora-wave-1 { animation-duration: 20s; }
    .aurora-wave-2 { animation-duration: 24s; }
    .aurora-halo { animation-duration: 22s; }
  }
</style>
